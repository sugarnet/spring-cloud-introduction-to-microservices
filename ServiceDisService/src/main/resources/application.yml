# Configure this Discovery Server
eureka:
  instance:
    hostname: localhost
  client: #not a client
    registerWithEureka: false
    fetchRegistry: false
    
# HTTP (Tomcat) port
server:
  port: 2345